# MOE Stage 1 Modules

Copy these files into your repository's `src/` directory:

- `validate_data.py`
- `build_features.py`

Expected raw file location:

```text
data/raw/
```

Expected output locations:

```text
data/outputs/validation_report.csv
data/engineered/data_features.csv
```
